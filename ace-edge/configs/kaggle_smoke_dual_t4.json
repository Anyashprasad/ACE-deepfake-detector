{
  "seed": 2401,
  "image_size": 224,
  "global_batch_size": 128,
  "epochs": 1,
  "learning_rate": 0.001,
  "reference_batch_size": 256,
  "weight_decay": 0.0001,
  "dropout": 0.2,
  "backbone_weights": "imagenet",
  "train_manifest": "DISCOVER_AT_RUNTIME",
  "validation_manifest": "DISCOVER_AT_RUNTIME",
  "unseen_generator_manifest": "DISCOVER_AT_RUNTIME",
  "output_dir": "/kaggle/working/ace-edge",
  "max_fp32_mib": 50.0,
  "locality_loss_weight": 0.25,
  "reliability_loss_weight": 0.0,
  "label_smoothing": 0.05,
  "cache_dataset": false,
  "num_parallel_calls": -1,
  "steps_per_epoch": 20,
  "benchmark_warmup_steps": 20,
  "benchmark_steps": 100,
  "benchmark_repetitions": 10,
  "benchmark_repetition_scope": "fresh_process",
  "benchmark_bootstrap_draws": 10000,
  "minimum_dual_gpu_speedup": 1.35,
  "max_output_gib": 2.0
}
